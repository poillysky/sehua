# backend/scripts

爬虫运维与识别进化相关脚本。

| 目录/约定 | 用途 |
|-----------|------|
| 根下具名脚本（如 `parse_evolve.py`） | **常驻运维**，可入库、可被文档引用 |
| `_` 前缀或一次性探查脚本 | 尽量放 `scratch/`（已 gitignore），勿堆在仓库根脚本树 |
| 产出日志 / CSV / JSON | 写到 `artifacts/` 或系统临时目录，不要提交 |

识别进化流程见 [`docs/识别进化流程.md`](../../docs/识别进化流程.md)。
