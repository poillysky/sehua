# 已改为在 docker-compose.nas.yml 里直接写死配置（无 ${VAR}）。
# 部署前请编辑该 yml，至少改：
#   - ghcr.io/<你的用户名>/sehuatang-*:latest
#   - POSTGRES_PASSWORD / INITIAL_ADMIN_PASSWORD
#   - 卷路径（建议改成 /vol1/1000/Docker/sehuatang/data/...）
#   - WEB_CRAWLER_PROXY、CORS_ORIGINS、SEARCH_FRONTEND_URL（按需）
#
# 本文件仅作备忘，compose 不再读取 .env。
